## 3.1 需求分析
- **解决什么问题**：传统用户在面对长篇学术论文、行业标准、技术手册时，存在“阅读耗时长、关键信息检索难、Ctrl+F 无法进行语义匹配”的痛点。
- **面向用户**：面临海量文献阅读需求的高校学生、科研人员及办公职场人。
- **核心价值**：提供“即传、即查、即答”的智能交互体验，实现秒级抽取长文档核心要点，大幅缩减信息获取成本。
- **AI 扮演的角色**：AI 在本项目中充当“高维语义检索器”与“高级阅读理解助手”。通过将文本块转化为 Vector Embedding，实现逻辑层面的语义相似度计算；通过调用大语言模型（LLM）对召回的知识切片进行归纳总结。选择 AI 而非规则的原因在于，人类自然语言提问具有极强的多样性，传统正则表达式或关键词匹配无法理解“同义词”和“上下文语境”，而大模型具备强大的泛化和推理总结能力。

## 3.2 方案设计
- **系统架构与数据流向**：
  1. 【数据准备流】：用户上传 PDF -> `PyPDFLoader` 文本提取 -> `RecursiveCharacterTextSplitter` 动态切块 -> `BAAI/bge-m3` 模型向量化 -> 写入本地 `FAISS` 数据库。
  2. 【问答推理流】：用户输入 Question -> 问题向量化 -> `FAISS` 进行 Top-3 相似度检索 -> 检出 Context 片段 -> 注入结构化 System Prompt -> 提交给 `Qwen2.5-7B` 模型 -> 前端渲染自然语言 Answer 并展开参考片段。
- **技术选型**：
  - **开发语言**：Python 3.9+
  - **交互框架**：Streamlit（组件化快速构建 AI 原型，利于演示）
  - **AI 编排框架**：LangChain（提供工业级的标准 RAG 链抽象接口）
  - **向量数据库**：FAISS（轻量级、高并发，适合单机免部署集成）
  - **大模型接入**：选用兼容 OpenAI 规范的国内云端 API（硅基流动平台），大模型采用 `Qwen/Qwen2.5-7B-Instruct`，向量模型采用 `BAAI/bge-m3`，保证零成本的高效运行。

## 3.3 关键实现说明
- **模型调用与封装**：代码将核心业务完全内聚于 `src/` 目录下。采用 `create_retrieval_chain` 自动化封装了检索和代入大模型的复杂逻辑。
- **Prompt 逻辑设计**：在 `src/rag_engine.py` 中，精心设计了防幻觉 Prompt。显式加入了约束指令（“如果你从上下文中找不到答案，请直接诚实地回答...切勿编造”），从底层工程逻辑上杜绝了 LLM 的“胡说八道”现象。
- **安全隔离**：项目通过 `.gitignore` 机制严格将 `.env` 隔离在本地，GitHub 上仅提交无害的 `.env.example` 占位说明文件，符合企业级安全发布规范。

## 3.4 测试结果与局限性分析
- **有效场景**：对于结构清晰、纯文本或表格密集的 PDF 报告（如财报、技术白皮书、论文），系统召回率极高，总结精准，多模态溯源页码展示准确。
- **失败情况与局限性**：
  1. 如果 PDF 中包含大量扫描版图片且未经过 OCR 处理，`PyPDF` 会提取到空文本，导致 AI 无法获取知识。
  2. 如果用户提出的问题需要进行全书宏观的跨章节超大范围数据统计，当前的 Top-K 局部文本块切片召回策略可能会丢失其余章节的关联背景。

## 3.5 个人收获与改进方向
- **个人收获**：通过本次 HW08 项目，我不仅掌握了 LangChain 框架和 RAG 技术的核心架构，更第一次完成了“需求分析 -> 模块解耦设计 -> 界面开发 -> 安全规范管控 -> 闭环测试”的完整 AI 工程师闭环。
- **改进方向**：
  1. 引入前端 OCR 预处理模块，解决扫描件 PDF 无法读取的问题。
  2. 引入 `ConversationBufferMemory` 链，使系统具备上下文记忆能力，支持多轮连续对话追问。